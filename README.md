file 1 >>
 simple Python script that demonstrates the use of functional programming tools: map, filter, and reduce from the Python standard library. The script performs the following operations on a list of integers:

Squares each number using map.

Filters the list to retain only odd squares using filter.

Sums the filtered odd squares using reduce.
